# Azure Blob Storage Configuration
# Choose one of the following authentication methods:

# Method 1: Connection String (Recommended for development)
AZURE_STORAGE_CONNECTION_STRING=DefaultEndpointsProtocol=https;AccountName=your_storage_account;AccountKey=your_account_key;EndpointSuffix=core.windows.net

# Method 2: Account Name and Key
# AZURE_STORAGE_ACCOUNT_NAME=your_storage_account_name
# AZURE_STORAGE_ACCOUNT_KEY=your_storage_account_key

# Method 3: Use Azure Default Credentials (Recommended for production)
# AZURE_STORAGE_ACCOUNT_NAME=your_storage_account_name
# Leave AZURE_STORAGE_CONNECTION_STRING and AZURE_STORAGE_ACCOUNT_KEY empty to use DefaultAzureCredential

# Container Configuration
AZURE_STORAGE_CONTAINER_NAME=input-files

# Optional: Custom endpoint (for Azure Government, China, etc.)
# AZURE_STORAGE_ENDPOINT_SUFFIX=core.windows.net

# FastAPI Configuration
FASTAPI_HOST=0.0.0.0
FASTAPI_PORT=8000
FASTAPI_RELOAD=false

# CORS Configuration
# Comma-separated list of allowed origins (use * for all origins in development)
CORS_ORIGINS=*
# Set to true if you need to send cookies or authentication headers
CORS_ALLOW_CREDENTIALS=true
# Comma-separated list of allowed methods
CORS_ALLOW_METHODS=GET,POST,PUT,DELETE,OPTIONS
# Comma-separated list of allowed headers
CORS_ALLOW_HEADERS=*

# Logging Configuration
LOG_LEVEL=INFO
LOG_ROTATION=100 MB

# Development Settings
DEBUG=false
ENVIRONMENT=production